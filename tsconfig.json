{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "**/*.js",
    "**/*.jsx",
    "image.d.ts" // Esta linha garante que o TypeScript reconheça a declaração de tipos para imagens.
  ]
}